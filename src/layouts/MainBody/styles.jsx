// @emotion
import { css } from "@emotion/css";

const ContentCss = css({
  height: "100%",
});

const MainBody = css({
  minWidth: 500,
  width: "calc(100% - 350px)",
  margin: "0 20px",
  height: "90vh",
  minHeight: 300,
});

export default MainBody;

export { ContentCss };
