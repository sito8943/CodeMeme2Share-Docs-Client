// @emotion
import { css } from "@emotion/css";

const MainBody = css({
  minWidth: 500,
  width: "calc(100% - 350px)",
  margin: "0 20px",
  height: "90vh",
  minHeight: 300,
});

export default MainBody;
